import { atom } from 'jotai';


export const seataAtom = atom([]);
export const seatbAtom = atom([]);
export const seatcAtom = atom([]);
export const seatdAtom = atom([]);
export const seateAtom = atom([]);
export const seatfAtom = atom([]); 